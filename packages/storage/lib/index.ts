import { createStorage, StorageType, type BaseStorage, SessionAccessLevel } from './base';

type TokenKey = 'gtk';

export { createStorage, StorageType, SessionAccessLevel, BaseStorage, type TokenKey };
